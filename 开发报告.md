

## 第一部分 · 实验内容

### 1. 总体架构

小程序目录按“页面 / 组件 / 数据 / 工具”分层：

- `pages/{index,create,detail,profile}` —— 四个功能板块（首页、制作、详情、我的）；
- `components/card-view` —— 最核心的“模板化卡片渲染引擎”，所有板块复用；
- `custom-tab-bar` —— 自定义三格底部导航（图标用图片资源替换，选中态与深色态同步切换）；
- `data/presets.js` —— 可配置的内容预设（标签词库、风格、比例、底色）；
- `utils/store.js` —— 本地数据服务层，统一封装账号、卡片、标签、收藏、留言、主题、排序；
- `utils/cardExporter.js` —— 用 Canvas 2D 把卡片渲染成图片，供保存与分享；
- `utils/util.js` —— 时间、ID 等工具。

设计上刻意让**页面只关心“展示与交互”**，把“数据从哪来、存到哪、按什么隔离”全部收敛进 `store`。后来做暗黑模式、账号切换、收藏排序时，这个约定让改动几乎都落在一两个文件里。

### 2. 板块一：制作卡片（创作端）

创作端是主入口，被设计成一条平缓的“表单 + 即时反馈”流程：

1. **照片**：一次可从相册多选，支持设封面、删图、点图全屏预览；照片多时还能点「⇅ 拖动排序 / 设封面」，长按把某张图拖到第一位，**第一张即封面**；
2. **版式与风格**：把“照片比例”与“背景风格”**解耦成两个独立维度**自由组合——比例决定照片区的宽高比（1:1 / 4:5 / 3:4 / 16:9 / 4:3 / 3:2），风格决定框体配色与装饰（极简白 / 雾蓝信纸 / 拍立得 / 胶片 / 深空 / 冷色撞色）；
3. **自定义背景**：在 6 种风格之外，还能从 10 种预设底色里挑，或**上传一张自己的图片做背景**（引擎自动叠加半透明遮罩保证文字可读）；
4. **标签**：从“内置 + 自建”词库里挑 1~4 个，首个作主标题；制作页只负责挑选，**词条的新增 / 改名 / 删除统一放到「我的 → 标签素材」**里管理；
5. **时间 / 地点**：快速填入或手写，作为卡片元信息；
6. **预留长文本**：为详情页“评论/留言”预留引导语；
7. **心情与贴纸**：选一个“心情色”给卡片定基调（卡片左上角显示成彩色小徽章），再从贴纸盘里点一枚 emoji 贴到照片上——**贴上后可以直接在实时预览里拖动摆位置，长按撕掉**，最多 8 枚。

列表右侧会随每一步变化**实时渲染成品预览**，所见即所得。制作页同时支持“编辑自己的卡”与“仿照他人的卡再做一张”（照片会被复制一份，归属清晰）。

### 3. 板块二：首页卡片墙（浏览 + 筛选）

首页把“看卡”体验做成两类结构：

- **时间线分组**：卡片按“日历日”分节，节头显示 `今天 / 昨天 / x月x日` 等字样，不同日期的内容被文字分隔开；
- **瀑布流双列**：由于卡片比例各不相同，简单“两列逐行”会在每行留下大空档。实现里按每张卡的**估算高度贪心放入较矮的一列**，形成两列自动平衡的瀑布流，列内间距一致、整体紧凑美观。

筛选系统参考电商“筛选 + 条件栏”的模式，做成三个彼此可叠加的维度：

- **标签**：点「＋ 标签筛选」打开**多选详细筛选页**，选项按卡片使用频率排序并带使用次数，支持“满足任一标签即显示”；已选标签以可删除胶囊回到顶部；
- **地点**：与标签一致的**多选详细筛选**，选项由“卡片里保存过的不同地点”自动生成，同样带使用次数；
- **时间**：不再是固定“今天/本周”，而是两个日期选择器，支持**用户自定义起始与终止时间**，结果标题随区间动态变化；
- **心情**：卡片用过的心情色会横排成一行（带使用次数，用得多的排前面），点一下只看该心情的卡片，再点取消。

每张卡可收藏（爱心高亮 + 轻震动），多图角标提示内有多张可轮播；卡片上的标签主标题独占一行、其余小标签**逐行排列**，读起来更清楚。首页顶部还放了一个「🎲 随机看一张」，随手抽一张卡片去看。

### 4. 板块三：卡片详情（浏览 + 长文本留言 + 导出分享）

详情页的展示逻辑做过一次调整：**首屏只留最想看的东西**——大卡片（含标签）、收藏、去评论，以及一个导出/分享按钮；创作者信息、预留长文本、评论输入与留言流都在**下滑之后**才出现，页面更干净，也不会一进来就被大段文字淹没。

留言依旧是长文本形态，按时间倒序排列，自己的留言在右侧高亮、气泡异色；**每条留言都可以被回复**，回复以二级评论的形式挂在留言下方，并会通知被回复的人；创作者本人可以编辑、删除，**「♻ 我也做一张」则是所有人都能用——包括作者自己**（照片会复制一份，做出一张真正属于自己的新卡片）。**点创作者或留言者的头像**，可以进入对方的个人主页，看看 TA 还做过哪些卡片。

这一版还补上了**导出与分享**：点 `↗` 会用 Canvas 2D 把当前卡片画成一张成品图——**多张照片会按网格合成进同一张图**（2 张并排、3 张一行、更多则按 2/3 列排布），再叠加主副标签、时间地点与水印，**贴纸与心情徽章也会按界面上同样的百分比位置画进去**，颜色随风格明暗自适应；出图后可以**保存到相册**、**直接发送给朋友**，照片多于一张时还能**另存全部原图**。同时页面接上了 `onShareAppMessage` / `onShareTimeline`，微信右上角「⋯」可以把卡片链接分享出去，分享封面就用刚导出的图片。

轮播上也做了一个小调整：手指一碰图片就**自动暂停**，交还给手动滑动，想继续自动播放再点右上角的播放键，避免“想翻看时它自己跑掉”。

另外做了一个纯好玩的小功能——**翻面**。点卡片左上角的「🔄 翻面」，卡片会 3D 翻转露出“背面”：主标签、余下的副标签、心情色、被多少人收藏、多少条留言、多少枚贴纸，然后是**完整的留言墙**（每一条留言连同二级回复都原样显示，一条不截断，内容多就在背面里滚动着看），最后是落款处的**制作者印章**和自己名字。背面用的是一套跟正面完全不同的信笺配色（深浅色模式各一版），翻过去像真的把一张实体卡翻过来。翻到背面时轮播会自动停下，点底部的提示条或左上角按钮都能翻回正面。

### 5. 板块四：我的（账号、拼图墙与个性化）

- **多账号**：支持创建 / 切换 / 删除本地账号。由于测试号没有真实登录态，身份用一个本地 `uid` 表示；收藏、卡片、标签词库、排序都按 `uid` 隔离，切换账号后互不串扰；
- **我制作的**：瀑布流双列展示，另有「⇅ 拖动排序」模式——点开后卡片变成单列可拖动的行，长按拖动即可自定义顺序，松手保存；
- **我的收藏**：做成了**拼图墙**。每张收藏卡按自身比例占据不同高度，带着 ±2.4° 的随机倾角，像贴在册子上的照片；点「✥ 编辑拼图」后可以**长按自由拖动**，卡片跟手移动、碰到别的位置就实时换位，右上角 `⤢ / ⤡` 还能把它放大成通栏或缩回半宽，位置与尺寸按账号记住；另外给了个「🎲 洗牌」一键重新布置；
- **标签词库管理**：点统计里的“标签素材”展开当前账号完整词库，可在此**新增、改名、删除**，并区分内置/自建；
- **互动反馈**：别人收藏、留言或回复你的卡片时，会在「🔔 互动反馈」里留下记录（带未读角标），可以直接跳到那张卡片，或去看对方的主页——让创作者知道“谁看过、谁喜欢”；
- **暗黑模式**：个人页一键切换浅/深色，选择持久化，所有页面即时生效。

### 6. 视觉与动效

- 整体是**夏日水色系**：浅天青底、水蓝主色，配日光金的小点缀；深浅两套配色都收在 CSS 变量里（`--bg/--surface/--ink/--line/--accent…`），换肤不用逐条改色；
- 界面有**体积感**：面板统一多层投影 + 内高光描边，胶囊与主按钮有浮起与按下的层次；
- **页面切换**（Tab 与进入子页）做了顺滑的淡入过渡，卡片本身有 `popIn` 入场；
- 首页圆环圆点、制作页光晕、个人页身份卡的多层光晕等**轻量装饰**，让以白卡为主的界面不至于单调；
- 收藏拼图墙的位置变化带缓动动画，拖动时卡片抬起加重阴影，拖完自动归位。

### 7. 贯穿性的小机制

- **照片持久化**：相册临时文件会被复制进 `USER_DATA_PATH/kk_photos`，删除卡片/换图时清理，避免临时目录被回收或残留；
- **旧数据兼容**：早期卡片只有单一 `template` 字段，读取时通过 `LEGACY_RATIO` 等映射到新的 `ratio/style`，老卡无需迁移即可继续显示；启动时还会跑一次**存档修复**——账号补上 `uid / 昵称 / 头像色`，卡片补上 `id / photos / phrases / messages[].replies / stickers` 等字段；**没有归属、或归属账号已不在本地列表里的老卡片会认领给当前账号**（否则它既不出现在「我制作的」里，作者本人也编辑不了），收藏表如果不是 `{uid: [cardId]}` 的形状也会被修正；
- **后台化分工**：页面 JS 不写任何 `wx.getStorageSync`，全部经由 `store`，后续换云端只动一个文件。

---

## 第二部分 · 总结与问题

### 8. 遇到的主要困难

1. **WXML 的表达式与结构限制**：`wx:if` 与 `wx:for` 不能写在同一节点；`wx:else` 必须紧跟 `wx:if`，中间夹个注释都会报错；像 `favMap[item.id]` 这种动态下标并不可靠，过滤器、函数调用也不被支持。

2. **样式隔离与主题**：组件样式默认隔离，页面级 CSS 变量能不能穿透进组件一度存疑；暗色模式还遇到过“`page` 元素背景没被变量覆盖”，导致首页看起来完全没生效。

3. **浮层与手势是反复踩坑的硬骨头**：`scroll-view` 不给确定高度就不会滚动；浮层打开后手指一滑，滚的却是**底层的页面**；自定义底部导航又在某些层叠关系下遮住浮层的底部按钮。

4. **多比例内容的排版**：一旦卡片比例各不相同，普通两列网格就会出现大量行间空档；而到了收藏拼图墙，还要再解决“自由拖动 + 位置实时重排 + 尺寸自适应”这一层。

5. **账号与“多用户”语义**：没有后端时“区分用户 / 切换账号”只能靠本地 uid，而且如果入口不明显，很容易被当成“功能没做”。

6. **图片与导出**：相册选到的只是临时文件，直接引用会在会话结束后失效；导出卡片又要处理本地路径加载、不同比例下的画布尺寸与文字可读性。

7. **“除了首页都白屏”这类问题很难定位**：页面 JS 在本地用假的 `wx` 跑得好好的，可真机上照样白；WXML 的编译错误、某个取不到数据的 `wx:if`、一次失败的 `setData`，表现出来都只是**一整片白**，看不出是谁的锅。

### 9. 对应的解决方法

| 问题 | 解决 |
|---|---|
| `wx:if/else/for` 限制 | 用 `<block wx:if>` 包裹 `wx:for`；`wx:else` 紧跟、不留注释；动态状态先在 JS 里预计算成每行字段（`faved/sel/on…`）再绑定 |
| 主题作用域 | 主题 token 定义在 `page` 与 `.page.dark`；给所有页面根 `.page` 显式 `background: var(--bg)`，让暗色真正作用到画布 |
| 浮层无法滚动 | 打开时用 `createSelectorQuery` 量出“屏幕 − 头部 − 底部”的可用高与内容实际高，给滚动区一个**精确 px 高度**：内容少不虚高，内容多才进入内部滚动 |
| 滑到底层页面 / 被底栏遮挡 | 遮罩与抽屉根加 `catchtouchmove` 拦手势；浮层打开时**临时隐藏底部导航**，关闭再恢复，从根上避免遮挡 |
| 不同比例的空档 | 改为**瀑布流**：按估算高度（照片占比 + 说明栏 + 底栏）贪心分入两列，两列自动平衡 |
| 收藏拼图墙的自由拖拽 | 瓦片绝对定位，拖动时用指针坐标做命中检测，命中别的位置就把数组重排并重新排版；位置与尺寸按账号持久化，切换尺寸即时重排 |
| 导出与分享 | 离屏 `Canvas 2D` 按卡片比例算画布高度，先把多张照片按网格依次加载、排版合成，再绘制文字后 `canvasToTempFilePath` 出图；保存走相册授权链路，分享用 `showShareImageMenu`，并补上页面分享接口 |
| 自动轮播与手动滑动打架 | swiper 绑 `bindtouchstart` 向页面抛事件，手指一碰就暂停自动播放，页面把控制权交还用户；需要自动播放再点按钮恢复 |
| 账号切换不明显 | 账号抽屉内联“＋新建输入框 + 一键快速新建”，整行可点即切换，空态给引导；切换后刷新全部列表与统计 |
| 互动如何“被看见” | 收藏、留言、回复都写入对应账号的反馈列表（`kk_notify_<uid>`），未读角标 + 反馈面板 + 直达卡片/主页；回复既通知卡片作者，也通知被回复的留言者 |
| 图片持久化 | `FileSystemManager.copyFile` 拷进用户目录再引用；`removeImages` 统一清理，且只删自己管理的前缀路径 |
| 旧存档打不开 | 老卡片缺字段时，WXML 里 `item.phrases[0].text`、`card.photos.length` 这类**深层次取值会直接让整页渲染失败**。改成**在 JS 里把封面、标题、照片数这些先算成字段**再绑定，视图层只做扁平取值；同时启动时统一跑一次存档修复（补字段、认领无主卡片、修正收藏表） |
| 开发工具报 `module '@swc/runtime/_array_with_holes.js' is not defined` | 开发者工具把 ES6 转 ES5 时，**数组解构**（`const [img] = arr`）会生成 `_sliced_to_array` + `_array_with_holes` 两个运行时辅助模块，而这版工具的运行时里没有打包后者，于是**模块加载就抛错、页面注册失败、整页空白**。规避办法是**不用解构、不用展开、不用 for...of**，改成下标取值与普通循环——本项目已全部改掉，并加了一条自动检查防止回退 |
| 整页白屏（页面根本没注册上） | `Page({ data: {...} })` 里的 `data` 是**执行到这句时立刻求值**的。我曾在 `data` 里写了 `moodList: presets.MOODS.map(...)`，一旦预设文件是旧版本（没有这个字段），这行就抛 `TypeError`，**`Page()` 直接中断、页面注册不上，表现就是整页白屏**——而且顶层静态标签也一起没了，比“某个绑定渲染失败”更彻底。改法：**`data` 里只放字面量或已算好的常量**，需要兼容的预设统一在文件顶部用带兜底的常量（`const MOODS = presets.MOODS || [内置默认]`） |
| “点了按钮没反应” | 微信的**页面栈最多 10 层**，`wx.navigateTo` 超限后会**静默失败**；而 `switchTab` 会清空页面栈，所以症状是“首页总是好的、别处时好时坏”。统一走 `utils/nav.js`：先 `navigateTo`，失败时自动改用 `redirectTo` 把目标页打开，用户永远到得了目的地 |
| 页面白屏不好查 | ① 把“可能编译不过”的写法全部清掉：`card-view` 里原本有 `wx:for` 与 `wx:if` 写在同一节点，改成**在 JS 里先算好要显示哪几行标签**再绑定；② **页面根节点不再用 `wx:if` 包住**，把判断挪到内容区，并给 `card`/`user` 这些数据准备安全占位，最差也只是内容空，不会整页白；③ `App.onError` / `onPageNotFound` 兜底打日志并回首页；④ 打不开时页面自己显示一句“这张卡片不存在”而不是留白 |

### 10. 未来改进空间

**数据与真实性**
- 接入微信**云开发**：云函数 `getWXContext().OPENID` 拿真实身份，收藏、标签、卡片入库并跨端同步；
- 完善权限与隐私链路：头像昵称走微信授权，删除时二次确认并回收云存储文件。

**分享的下一阶段**
- 现在已能把卡片导出成图片保存/发送，也可以分享卡片链接；往后可以给每张卡生成**小程序码/二维码**，扫码直达详情；
- 支持“合辑分享”——把一整个拼图墙排版导出一张长图。

**内容与创作**
- 模板引擎再升级：允许自定义字体、字号、颜色，沉淀成“我的背景库 / 模板库”；
- 标签支持排序、编辑与用量分析，“仿照”可一键继承版式再替换照片；
- 卡片上已经能贴 emoji 贴纸了，下一步想做**便签与手写涂鸦**（在照片上手绘一笔），并把贴纸做成一套可自己收集的“贴纸包”；拼图墙也能贴。

**体验与工程**
- 引入状态管理与 TypeScript，抽出筛选/主题的通用 store；
- 补图片压缩、骨架屏与多机型视觉回归测试，支持系统深浅色自动跟随；
- 把动画与装饰沉淀为可配置的设计系统，统一风格变量与组件规范。


